import { YearStatisticsModel } from './year-statistics.model';

/**
 * @class SportModel
 * class used to handle the model of the sport application
 */
export class SportModel {
    public name: string;
    public years: YearStatisticsModel[];
}